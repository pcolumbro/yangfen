export const convertGtoMg = (grams: number): number => {
	return grams * 1000;
}

export const convertMgToG = (miligrams: number): number => {
	return miligrams / 1000;
}

export const convertMcgToMg = (mcg: number): number => {
	return mcg / 1000;
}